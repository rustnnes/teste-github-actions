name: Main CI

on:
  workflow_dispatch

jobs:
  test:
    env:
      FNAME: diego
    defaults:
      F_NAME: diego1

    runs-on: ubuntu-latest

    steps:
    - name: gogogo
      run: |
        echo ${{ env.F_NAME }}
