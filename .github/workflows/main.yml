name: CI

on:
  workflow_dispatch

jobs:
  test:
    env:
      F_NAME: diego
    defaults:
      F_NAME: diego1

    runs-on: ubuntu-latest

    steps:
    - name: gogogo
      run: |
        echo ${{ env.F_NAME }}
